{
  "name": "turbo-bun-astro-trpc-starter",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "serve": "turbo run serve --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test --concurrency=1",
    "check": "turbo run check",
    "format": "prettier -w ."
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint-config-preset": "workspace:^",
    "prettier-plugin-preset": "workspace:^",
    "tsconfig-preset": "workspace:^",
    "svelte": "^3.55.1",
    "turbo": "^1.8.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@7.22.0",
  "pnpm": {}
}
